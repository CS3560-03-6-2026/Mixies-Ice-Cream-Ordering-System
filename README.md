# Mixies-Ice-Cream-Ordering-System
CS3560 Group 6 Project

## Running the Project

1. Make sure MySQL is running with account = root and password = cs3560mixies
2. Compile: javac -cp "lib/mysql-connector-j-9.7.0.jar" -d bin src/*.java
3. Run: java -cp "bin;lib/mysql-connector-j-9.7.0.jar" Main
